true: bin_annot
false: package(adpkg), package(eliom.ocamlbuild)
true: \
    package(fmt), \
    package(prime.unstable), \
    package(ptime)
<web/server/*>: \
    package(config-file), \
    package(eliom.server), \
    package(fd-send-recv), \
    package(ptime.clock.os), \
    package(x509)
<web/client/*>: \
    package(eliom.client), \
    package(ptime.clock.jsoo)
